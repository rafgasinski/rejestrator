<UserControl x:Class="rejestrator.Views.AdminEmployees"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:customcontrols="clr-namespace:rejestrator.CustomControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:utils="clr-namespace:rejestrator.Utils"
             xmlns:local="clr-namespace:rejestrator.Views" xmlns:viewmodels="clr-namespace:rejestrator.Viewmodels"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="Visibility" TargetType="{x:Type TextBox}">
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="GridViewColumnHeaderStyle1" TargetType="{x:Type GridViewColumnHeader}">
                <Setter Property="Background" Value="Black"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Grid SnapsToDevicePixels="True">
                                <Border BorderThickness="0,1,0,1" BorderBrush="{TemplateBinding Border.BorderBrush}" Background="{TemplateBinding Panel.Background}" Name="HeaderBorder">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition MaxHeight="7" />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="#FFE3F7FF" Name="UpperHighlight" Visibility="Collapsed" />
                                        <Border Padding="{TemplateBinding Control.Padding}" Grid.RowSpan="2">
                                            <ContentPresenter RecognizesAccessKey="True" Content="{TemplateBinding ContentControl.Content}" 
                                                          ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" 
                                                          ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}" 
                                                          Name="HeaderContent" Margin="0,0,0,1" 
                                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" 
                                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" 
                                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="true">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="Black"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>

    </UserControl.Resources>

    <materialDesign:DialogHost Identifier="AddDialogHost" IsOpen="{Binding IsDialogAddOpen}">
        <materialDesign:DialogHost.DialogContentTemplate>
            <DataTemplate DataType="{x:Type viewmodels:Employee}">
                <StackPanel Margin="30">

                    <TextBox PreviewTextInput="TextBox_PreviewTextInput" x:Name="IDBox" Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding ID}" FontSize="16" materialDesign:HintAssist.Hint="Id (4 cyfry)" MaxLength="4" />
                    <TextBox PreviewTextInput="TextBox_PreviewTextInput" x:Name="PinBox" Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding Pin}" FontSize="16" materialDesign:HintAssist.Hint="Pin (4 cyfry)" MaxLength="4"/>
                    <TextBox x:Name="NameBox" Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding Name}" FontSize="16" materialDesign:HintAssist.Hint="Imię" />
                    <TextBox x:Name="SurnameBox" Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding Surname}" FontSize="16" materialDesign:HintAssist.Hint="Nazwisko" />
                    <ComboBox x:Name="ComboBoxEmployeeWorktime" Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" MaxDropDownHeight="30" FontSize="16" materialDesign:HintAssist.Hint="Tryb pracy" ItemsSource="{Binding Path=TwoWays}" SelectedItem="{Binding SelectedShift}" IsSynchronizedWithCurrentItem="True"/>

                    <ComboBox x:Name="ComboBox" Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" MaxDropDownHeight="80" FontSize="16" materialDesign:HintAssist.Hint="Pracownik" ItemsSource="{Binding Path=Queries}" SelectedItem="{Binding SelectedEmployee}" IsSynchronizedWithCurrentItem="True"/>
                    <TextBox x:Name="TaskBox" Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding Task}" FontSize="16" materialDesign:HintAssist.Hint="Zadanie" />

                    <TextBox PreviewTextInput="TextBox_PreviewTextInput" x:Name="IDAdminBox" Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding IDadmin}" FontSize="16" materialDesign:HintAssist.Hint="Id (4 cyfry)" MaxLength="4"/>
                    <TextBox x:Name="UsernameBox" Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding AdminUsername}" FontSize="16" materialDesign:HintAssist.Hint="Nazwa użytkownika"/>
                    <PasswordBox x:Name="AdminPasswrodBox" Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" utils:PasswordBoxAssistant.BindPassword="True" utils:PasswordBoxAssistant.BoundPassword="{Binding Path=AdminPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="16" materialDesign:HintAssist.Hint="Hasło"/>
                    <TextBox x:Name="AdminNameBox" Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding AdminName}" FontSize="16" materialDesign:HintAssist.Hint="Imię" />
                    <TextBox x:Name="AdminSurnameBox" Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Text="{Binding AdminSurname}" FontSize="16" materialDesign:HintAssist.Hint="Nazwisko" />

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button 
                                IsDefault="True"
                                Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                Width="90"
                                Height="65"
                                ToolTip="Dodaj pracownika"
                                Command="{Binding EmployeeAddViewCommand}">
                            <Image Source="/rejestrator;component/Assets/profile-user.png"
                                       Stretch="Fill"/>
                        </Button>
                        <Button
                                IsDefault="False"
                                Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                Width="90"
                                Height="65"
                                Margin="10,0,0,0"
                                Grid.Column="1"
                                ToolTip="Dodaj zadanie"
                                Command="{Binding TaskAddViewCommand}">
                            <Image Source="/rejestrator;component/Assets/clipboard.png"
                                       Stretch="Fill" />
                        </Button>
                        <Button
                                IsDefault="False"
                                Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                Width="90"
                                Height="65"
                                Margin="10,0,0,0"
                                Grid.Column="2"
                                ToolTip="Dodaj admina"
                                Command="{Binding AddAdmindCommand}">
                            <Image Source="/rejestrator;component/Assets/settings.png"
                                       Stretch="Fill" />
                        </Button>
                        <Button
                                IsCancel="True"
                                Visibility="{Binding ChangeControlVisibility2, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                Width="90"
                                Height="65"
                                Margin="10,0,0,0"
                                Grid.Column="3"
                                Command="{Binding CloseDialogAdd}">
                            <Image Source="/rejestrator;component/Assets/cancel1.png"
                                       Stretch="Fill" />
                        </Button>

                        <Grid Margin="0,0,0,0" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <Button Content="_Dodaj" 
                                Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                IsDefault="False"
                                Width="80"
                                Grid.Column="0"
                                Command="{Binding CloseDialogAddAndAddToDatabase}"/>
                            <Button Content="_Anuluj" 
                                Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                IsCancel="True"
                                Width="80"
                                Margin="10,0,0,0"
                                Grid.Column="1"
                                Command="{Binding CloseDialogAdd}"/>
                            <Button Content="_Dodaj" 
                                Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                IsDefault="False"
                                Width="80"
                                Grid.Column="0"
                                Command="{Binding CloseDialogAddAndAddToDatabase}"/>
                            <Button Content="_Anuluj" 
                                Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                IsCancel="True"
                                Width="80"
                                Margin="10,0,0,0"
                                Grid.Column="1"
                                Command="{Binding CloseDialogAdd}"/>
                            <Button Content="_Dodaj" 
                                Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                IsDefault="False"
                                Width="80"
                                Grid.Column="0"
                                Command="{Binding CloseDialogAddAndAddToDatabase}"/>
                            <Button Content="_Anuluj" 
                                Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                IsCancel="True"
                                Width="80"
                                Margin="10,0,0,0"
                                Grid.Column="1"
                                Command="{Binding CloseDialogAdd}"/>

                        </Grid>
                    </Grid>
                </StackPanel>
            </DataTemplate>
        </materialDesign:DialogHost.DialogContentTemplate>

        <Grid Background="Black">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <customcontrols:Menu Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Canvas.ZIndex="3"/>

            <Border CornerRadius="10" BorderThickness="1" BorderBrush="Black" Background="White" Grid.Column="0" Grid.ColumnSpan="2" Width="300" Height="50" Margin="70,10,140,0">
                <ComboBox Grid.Row="0" Background="White" FontFamily="TW Cen MT" Text="{Binding SelectedEmployee}"
                   FontSize="26" Foreground="Black" MaxDropDownHeight="135" Width="290"
                      ItemsSource="{Binding Path=ListOfEmployees}" SelectedItem="{Binding SelectedEmployee}" IsSynchronizedWithCurrentItem="True" Canvas.ZIndex="4" IsEnabled="{Binding EmployeeListEnabled}">
                </ComboBox>
            </Border>

            <customcontrols:EditButton Grid.ColumnSpan="4" Grid.RowSpan="3" Canvas.ZIndex="-1" DataContext="{Binding Instance}"/>

            <Button Command="{Binding ShowDeletePanel}" Background="Transparent" Cursor="Hand" BorderThickness="0" ToolTip="Usuń" Grid.Row="0" Grid.Column="1" Height="35" Width="60" Margin="214,20,49,10">
                <StackPanel Orientation="Horizontal">

                    <Image Source="/rejestrator;component/Assets/delete.png"
                                       Stretch="Fill"
                                       Margin="0,0,0,0"/>
                </StackPanel>
            </Button>

            <Border CornerRadius="10" BorderThickness="1" BorderBrush="Black" Background="White" Width="300" Height="130" Grid.RowSpan="4" Grid.ColumnSpan="4" Canvas.ZIndex="5" Visibility="{Binding ChangeControlVisibility4,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Image Source="/rejestrator;component/Assets/padlock.png" Width="32" Height="32" Grid.RowSpan="2" Margin="20,0" />

                    <Label FontFamily="TW Cen MT" FontSize="13" Grid.Column="1" Margin="0,0,10,0" Content="Aby potwierdzić wpisz hasło:"/>
                    <PasswordBox utils:PasswordBoxAssistant.BindPassword="True" utils:PasswordBoxAssistant.BoundPassword="{Binding Path=PasswordConfirm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="13" materialDesign:HintAssist.Hint="Hasło" Grid.Column="1" Grid.Row="1" Margin="3,0,10,0"/>

                    <WrapPanel Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button FontFamily="TW Cen MT" IsDefault="True" MinWidth="60" Margin="0,0,10,0" Content="Ok" Command="{Binding DeleteEmployee}" Background="Gainsboro"/>
                        <Button FontFamily="TW Cen MT" IsCancel="True" MinWidth="50" Margin="0,0,10,0" Content="Anuluj" Command="{Binding HideDeletePanel}" Background="Gainsboro"/>
                    </WrapPanel>
                </Grid>
            </Border>

            <TextBlock Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Canvas.ZIndex="1" Margin="75,0,0,0"
                   Foreground="White"
                   FontFamily="TW Cen MT"
                   FontSize="20"
                   Text="Logowania:" />

            <TextBlock Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Canvas.ZIndex="1" Margin="150,0,0,0"
                   Foreground="White"
                   FontFamily="TW Cen MT"
                   FontSize="20"
                   Text="Przydzielone zadania:" />

            <TextBlock Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Canvas.ZIndex="1" Margin="75,0,0,0"
                   Foreground="White"
                   FontFamily="TW Cen MT"
                   FontSize="20"
                   Text="Zadania w toku:" />

            <TextBlock Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Canvas.ZIndex="1" Margin="75,0,0,0"
                   Foreground="White"
                   FontFamily="TW Cen MT"
                   FontSize="20"
                   Text="Zakończone zadania:" />


            <customcontrols:FullNameLabel Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="3" HorizontalAlignment="Right" Margin="15,15,10,15"/>

            <Button Command="{Binding SwitchPage}" Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="55" Background="Black" Grid.Row="1" Grid.Column="3" Cursor="Hand" BorderThickness="0" Height="30" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal">

                    <Image Source="/rejestrator;component/Assets/right-arrow.png"
                                       Stretch="Fill"
                                       Margin="0,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding SwitchPage2}" Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="55" Background="Black" Grid.Row="1" Grid.Column="3" Cursor="Hand" BorderThickness="0" Height="30" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal">

                    <Image Source="/rejestrator;component/Assets/right-arrow.png"
                                       Stretch="Fill"
                                       Margin="0,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding SwitchBackPage}" Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="270,0,0,0" Width="55" Background="Black" Grid.Row="1" Grid.Column="2" Cursor="Hand" BorderThickness="0" Height="30">
                <StackPanel Orientation="Horizontal">

                    <Image Source="/rejestrator;component/Assets/left-arrow.png"
                                       Stretch="Fill"
                                       Margin="0,0,0,0"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding SwitchBackPage2}" Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="270,0,0,0" Width="55" Background="Black" Grid.Row="1" Grid.Column="2" Cursor="Hand" BorderThickness="0" Height="30">
                <StackPanel Orientation="Horizontal">

                    <Image Source="/rejestrator;component/Assets/left-arrow.png"
                                       Stretch="Fill"
                                       Margin="0,0,0,0"/>
                </StackPanel>
            </Button>

            <ListView Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="Black" Foreground="White" FontFamily="TW Cen MT" FontSize="14" Margin="78,10,227,0"
            BorderThickness="0" Width="230"
            ItemsSource="{Binding Dates}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border BorderBrush="Transparent" BorderThickness="0" Background="{TemplateBinding Background}">
                                        <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Width="Auto" Margin="0,10,0,0" Content="{TemplateBinding Content}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle1}">
                        <GridViewColumn Width="220">
                            <GridViewColumnHeader FontSize="16" Content="Data"/>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <ListView Visibility="{Binding ChangeControlVisibility,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Background="Black" Foreground="White" FontFamily="TW Cen MT" FontSize="14" Margin="150,10,40,0"
            BorderThickness="0" Width="500"
            ItemsSource="{Binding Tasks}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border BorderBrush="Transparent" BorderThickness="0" Background="{TemplateBinding Background}">
                                        <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Width="Auto" Margin="0,10,0,0" Content="{TemplateBinding Content}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle1}">
                        <GridViewColumn Width="480">
                            <GridViewColumnHeader FontSize="16" Content="Zadanie"/>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <ListView Visibility="{Binding ChangeControlVisibility2,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Background="Black" Foreground="White" FontFamily="TW Cen MT" FontSize="1" Margin="75,10,0,0"
            BorderThickness="0" Width="830" Grid.Column="0" Grid.ColumnSpan="4"
            ItemsSource="{Binding TasksInProgress}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border BorderBrush="Transparent" BorderThickness="0" Background="{TemplateBinding Background}">
                                        <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Width="Auto" Margin="0,10,0,0" Content="{TemplateBinding Content}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle1}">
                        <GridViewColumn Width="520">
                            <GridViewColumnHeader FontSize="16" Content="Zadanie"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 50 0" Text="{Binding Task}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="150">
                            <GridViewColumnHeader FontSize="16" Content="Start"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 50 0" Text="{Binding Date}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <ListView Visibility="{Binding ChangeControlVisibility3,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Background="Black" Foreground="White" FontFamily="TW Cen MT" FontSize="12" Margin="75,10,10,0"
            BorderThickness="0" Width="810" Grid.Column="0" Grid.ColumnSpan="4"
            ItemsSource="{Binding TasksDone}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border BorderBrush="Transparent" BorderThickness="0" Background="{TemplateBinding Background}">
                                        <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Width="Auto" Margin="0,10,0,0" Content="{TemplateBinding Content}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle1}">
                        <GridViewColumn Width="400">
                            <GridViewColumnHeader FontSize="16" Content="Zadanie"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 50 0" Text="{Binding Task}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="150">
                            <GridViewColumnHeader FontSize="16" Content="Start"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 50 0" Text="{Binding DateStart}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="150">
                            <GridViewColumnHeader FontSize="16" Content="Koniec"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 50 0" Text="{Binding DateEnd}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="80">
                            <GridViewColumnHeader FontSize="16" Content="Czas"/>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Padding="0 0 50 0" Text="{Binding Time}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>

    </materialDesign:DialogHost>
</UserControl>
